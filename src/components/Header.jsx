// TODO Iteration 8 (bonus): define Header
// - Receives a `count` number as a prop
// - Renders a <header> with the title "IronDex"
// - If count > 0, shows "<count> pokemon en la lista"
// - If count === 0, shows "Sin pokemon disponibles"

function Header(/* TODO destructure props here */) {
  return (
    <header>
      <h1>IronDex</h1>
      {/* TODO conditional rendering based on count */}
    </header>
  );
}

export default Header;
